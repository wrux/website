---
import Layout from '~/layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
import SocialIcon from '~/components/SocialIcon.astro';
import { person, socialLinks } from '~/data/site';

const homepageSummary =
  'I work with teams that need dependable frontend delivery: clear interfaces, maintainable React systems, content-heavy sites, and product builds that keep moving after launch.';

const homepageSocialLinks = socialLinks.filter(
  (link) => link.href !== person.website,
);
---

<Layout
  title="Software Engineer & Freelancer Building Digital Products | wrux.com"
  description="Software engineer and freelancer building digital products, including my own project Kacet. Focused on clean code, thoughtful design, and practical solutions."
>
  <div class="grid gap-8 md:grid-cols-[minmax(0,2fr)_minmax(0,1.15fr)]">
    <section
      class="terminal-card p-6 border-b-0 border-l-0 border-r-0 rounded-none"
    >
      <h2
        class="flex items-center gap-2 text-lg font-semibold text-[var(--text)]"
      >
        <Icon
          name="lucide:file-text"
          size={20}
          class="text-[var(--accent)] print:text-black"
        />
        Summary
      </h2>
      <div class="max-w-prose">
        <p class="mt-3 text-base leading-relaxed text-[var(--text-muted)]">
          {homepageSummary}
        </p>
        <p class="mt-4 text-base leading-relaxed text-[var(--text-muted)]">
          {person.bio.split('callum.co.uk')[0]} on
          <a href="https://callum.co.uk" target="_blank" class="link-underline"
            >callum.co.uk</a
          >.
        </p>
      </div>
    </section>

    <div
      class="terminal-card p-6 border-l-0 border-r-0 rounded-none lg:border-l"
    >
      <h2
        class="flex items-center gap-2 text-lg font-semibold text-[var(--text)]"
      >
        <Icon
          name="lucide:globe"
          size={20}
          class="text-[var(--accent)] print:text-black"
        />
        Online
      </h2>
      <ul class="mt-4 flex flex-col gap-3 text-sm text-[var(--text-muted)]">
        {
          homepageSocialLinks.map((link) => (
            <li class="flex flex-col justify-between gap-3 min-w-0">
              <span class="flex items-center gap-2">
                <SocialIcon
                  name={link.name}
                  size={16}
                  class="text-[var(--accent)] print:text-black shrink-0"
                />
                {link.label}
              </span>
              <a
                href={link.href}
                target="_blank"
                rel="nofollow noreferrer"
                class="link-hover flex items-center gap-1.5 truncate min-w-0"
                title={link.href}
              >
                <span class="truncate">{link.href}</span>
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</Layout>
